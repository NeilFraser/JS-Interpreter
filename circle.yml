machine:
  node:
    version: 6.9
  environment:
    PATH: "${PATH}:${HOME}/${CIRCLE_PROJECT_REPONAME}/node_modules/.bin"
dependencies:
  override:
    - yarn
  cache_directories:
    - ~/.cache/yarn
    - tests/test262
test:
  override:
    - tests/test-on-circle.sh:
        parallel: true
